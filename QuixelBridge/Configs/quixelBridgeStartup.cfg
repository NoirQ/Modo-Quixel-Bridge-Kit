<?xml version="1.0" encoding="utf-8"?>
<configuration>
  <!-- run at startup. Remove this chunk if you don't need that. -->
  <atom type="StartupCommands">
    <list type="LateCommand">quixelBridge.start</list>
  </atom>
</configuration>
